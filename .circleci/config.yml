version: 2

jobs:
  build:
    docker:
      - image: circleci/node:11.14 
    working_directory: ~/cko-node-sdk
    steps:
      - checkout
      - run:
          name: Install
          command: npm i
      - run:
          name: Run Tests
          command: npm test
      - store_artifacts:
          path: coverage
